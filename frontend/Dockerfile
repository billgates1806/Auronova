# ==========================================================
# AURONOVA Frontend — Simple nginx Dockerfile
# Serves static files (HTML/CSS/JS)
# ==========================================================
FROM nginx:alpine

COPY . /usr/share/nginx/html/

EXPOSE 80
